name: peopletraffic_uc
version: '1.0.0'
config-version: 2
profile: peopletraffic_uc
model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
docs-paths: ["docs"]
target-path: "target"
clean-targets: ["target"]

models:
  peopletraffic_uc:
    bronze:
      +materialized: table
      +schema: bronze
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
